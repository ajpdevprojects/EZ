# apps/web

The EZ Next.js application. See the [repository README](../../README.md)
for setup and common commands, and `.env.example` for the environment
variables this app reads (Supabase, AI providers).
